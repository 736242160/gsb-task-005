我想让你用纯 Python 帮我从零实现一个轻量级的双协议多路复用内存缓存服务原型，并且不要引入任何第三方依赖，只能使用 Python 官方标准库。交付物必须是一个可直接单文件运行的脚本，能够同时支持原生轻量 HTTP（用于在浏览器中直接查看键值状态、命中率统计与内存占用）以及原始 TCP 纯文本协议的高性能吞吐，同时自带完整的并发与数据一致性单测。
这个原型的核心难点在于严苛的细粒度并发安全与高频淘汰算法。你需要完整实现带分段锁（Segmented Locking）或无死锁读写锁机制的并发安全存储引擎，保证海量高频读写下不存在全局大锁竞争。在此基础上，必须实现支持 TTL 过期淘汰以及高并发低开销的近似 LRU 或 2-Queue 淘汰策略，淘汰与清理过程必须是完全线程安全的，严禁出现内存泄漏、竞态条件或超额穿透。
所有的并发网络 IO 处理必须完全基于标准库内置的异步或套接字多路复用机制，杜绝因为慢客户端连接导致工作线程枯竭。服务启动后在后台并发模拟随机读写压测与过期淘汰，同时在本地监听端口，既能在浏览器端流畅刷新查看各项原子计数指标，又能直接跑通并发一致性自测。请直接给出逻辑严密、无死锁且开箱即用的完整原生代码。
